{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "scons cpu test",
            "type": "shell",
            "command": "scons",
            "args": [
                "bin/test_Cpu"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$msCompile" // is this the one for c++?
            ]
        }
    ]
}